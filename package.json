{
    "name": "investing-sim",
    "private": true,
    "engines": { "node": "20.19.0" },
    "workspaces": ["frontend", "backend"],
    "scripts": {
        "build": "npm ci && npm ci -w backend --omit=dev && npm run build -w frontend",
        "start": "npm run start -w backend"
    }
}